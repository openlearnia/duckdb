diff --git a/src/s3/s3_settings.cpp b/src/s3/s3_settings.cpp
index 536c708..944cded 100644
--- a/src/s3/s3_settings.cpp
+++ b/src/s3/s3_settings.cpp
@@ -102,7 +102,9 @@ idx_t S3UploadConfig::PartSize(idx_t reserved_parts) const {
 	D_ASSERT(initial_part_size >= MIN_MULTIPART_PART_SIZE);
 	D_ASSERT(initial_part_size <= MAX_MULTIPART_PART_SIZE);
 	D_ASSERT(growth_interval > 0);
-	return S3UploadSizing::PartSize(initial_part_size, growth_interval, reserved_parts);
+	// S3-compatible stores such as Cloudflare R2 require all non-trailing
+	// multipart parts to have the same length. Keep a fixed size per upload.
+	return initial_part_size;
 }
 
 bool S3UploadConfig::HasPartCapacity(idx_t reserved_parts) const {
diff --git a/src/s3/s3_upload_session.cpp b/src/s3/s3_upload_session.cpp
index 74f29d3..3c50881 100644
--- a/src/s3/s3_upload_session.cpp
+++ b/src/s3/s3_upload_session.cpp
@@ -325,7 +325,9 @@ S3UploadSession::PreparedWrite S3UploadSession::PrepareWrite(const_data_ptr_t da
 					input_offset += copied;
 					break;
 				}
-				auto direct_size = MinValue<idx_t>(remaining, S3UploadConfig::MAX_MULTIPART_PART_SIZE);
+				// Never emit a short or variably-sized non-trailing part. The final
+				// buffered part is the only part allowed to be shorter.
+				auto direct_size = MinValue<idx_t>(remaining, part_size);
 				ReservePart(result, data + input_offset, direct_size);
 				input_offset += direct_size;
 			}
